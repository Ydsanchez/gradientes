import 'package:flutter/material.dart';

class BuscarG extends StatelessWidget {
  const BuscarG({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("BUSCAR g"),
      ),
    );
  }
}