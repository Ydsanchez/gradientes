import 'package:flutter/material.dart';

class BuscarPtg extends StatelessWidget {
  const BuscarPtg({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("BUSCAR Ptg"),
      ),
    );
  }
}