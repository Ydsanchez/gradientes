import 'package:flutter/material.dart';

class BuscarN extends StatelessWidget {
  const BuscarN({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("BUSCAR n"),
      ),
    );
  }
}