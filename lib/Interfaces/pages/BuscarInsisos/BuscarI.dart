import 'package:flutter/material.dart';

class BuscarI extends StatelessWidget {
  const BuscarI({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("BUSCAR i"),
      ),
    );
  }
}