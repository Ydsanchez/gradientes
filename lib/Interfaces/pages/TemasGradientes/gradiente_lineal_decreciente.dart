import 'package:flutter/material.dart';

class GradientesLinealDecreciente extends StatelessWidget {
  const GradientesLinealDecreciente({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Gradientes Lineal Decreciente"),
      ),
    );
  }
}