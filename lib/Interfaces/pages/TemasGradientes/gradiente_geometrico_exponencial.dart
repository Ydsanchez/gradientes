import 'package:flutter/material.dart';

class GradienteGeometricoExponencial extends StatelessWidget {
  const GradienteGeometricoExponencial({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Gradientes Geometrico Exponencial"),
      ),
    );
  }
}