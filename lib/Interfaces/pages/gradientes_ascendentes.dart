import 'package:flutter/material.dart';

class Gradientes_Ascendentes extends StatelessWidget {
  const Gradientes_Ascendentes({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Gradientes Ascendentes"),
      ),
    );
  }
}
